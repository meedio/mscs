Okay we need to create a full view of documents to get a look at the current state of the matrixrtc msc in the matrix ecosystem.

We need to collect all of the PRs, all of the documentation available regarding this msc and the pending mscs, so delayed event stuff, slots, sticky events and then also for supporting evidence the multi-sfu

This should be all aggregrated in a directory, that has all of the inputs that we will use as evidence. 

Also we need to see what the msc pipeline looks like to be able to contribute, what is the lifecycle what is the main place where resources exist. how to start contributing.

And then mapping the current status of all those mscs like slots and sticky events.

so let's put it in:
lifecycle.md - for how we mscs work and go on
name-msc.md - for each of the pending mscs saying what is their state in the current lifecyvcle.
dependency.md - how mscs are interconnected and what order they need to be completed in to finish. 
input-directory - where we save all of the PRs and the documentations so we can reference it to substantiate every claim.
gaps.md - information that does not live anywhere that is blocking our research to see what is next. We can ask the creators directly and fill in these gaps to know the current status of everything.